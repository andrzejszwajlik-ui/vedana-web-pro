import type { VisitAnalysisData } from "./types";

const labels: Array<[keyof VisitAnalysisData, string]> = [["functionalProblem", "Problem funkcjonalny"], ["systemChangeHistory", "Historia zmiany systemu"], ["examinationSignals", "Sygnały z badania"], ["stoppingPoint", "Miejsce zatrzymania"], ["appliedStimulus", "Zastosowany bodziec"], ["localResponse", "Odpowiedź miejscowa"], ["globalResponse", "Odpowiedź globalna"], ["retest", "Retest"], ["unknownInformation", "Informacje nieznane"], ["nextDirection", "Dalszy kierunek"]];
export function VisitAnalysis({ analysis }: { analysis: VisitAnalysisData }) { return <section className="rounded-2xl border border-teal-200 bg-white p-5 shadow-sm"><h2 className="mb-5 text-sm font-semibold uppercase tracking-wider text-teal-800">4. Analiza po wizycie</h2><div className="grid gap-3 md:grid-cols-2">{labels.map(([key, label]) => <div key={key} className="rounded-xl bg-slate-50 p-4"><h3 className="text-sm font-semibold text-slate-800">{label}</h3>{analysis[key].length ? <ul className="mt-2 list-disc space-y-1 pl-4 text-sm text-slate-600">{analysis[key].map((text, i) => <li key={i}>{text}</li>)}</ul> : <p className="mt-2 text-sm italic text-slate-400">Brak zatwierdzonych informacji</p>}</div>)}</div></section>; }
